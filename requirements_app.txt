# Dependências apenas para o dashboard Streamlit (app.py)
# Use este arquivo ao fazer deploy no Streamlit Community Cloud:
#   Advanced settings → "requirements file" → requirements_app.txt
#
# NÃO inclui playwright, curl-cffi, fake-useragent nem tenacity
# pois esses são usados somente pelo coletor (main.py).

streamlit>=1.35.0
plotly>=5.20.0
pandas>=2.2.2
supabase>=2.3.0
python-dotenv==1.0.1
loguru==0.7.2
anthropic>=0.40.0

# Brief de mais vendidos renderizado na página 🥇 Mais Vendidos: quando o
# .md do dia não está na máquina do painel, ele é remontado com
# `DataFrame.to_markdown`, que exige tabulate. Ver docs/BESTSELLERS.md.
tabulate>=0.9.0

# Histórico frio (Parquet no Google Drive). O dashboard costura a janela quente
# do Supabase com o histórico — sem estas libs, `_history_gap_fill` falha no
# import, cai no except e o painel exibe só o que o banco devolveu, sem erro
# visível. Ver docs/HISTORICO_DRIVE.md.
pyarrow>=15.0.0
google-api-python-client>=2.100.0
google-auth>=2.30.0
google-auth-oauthlib>=1.2.0
