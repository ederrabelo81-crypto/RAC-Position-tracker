node_modules/
dist/
.env
data/*.csv
logs/*.log
*.js.map

# Sessões de scraper — cookies/tokens sensíveis (SPC_*, csrftoken).
# Geradas em runtime via capture-session.ts; NUNCA versionar.
sessions/*.json
shopee_chrome.json
