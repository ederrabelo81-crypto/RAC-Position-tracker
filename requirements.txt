playwright>=1.50.0
# Fork do Playwright com patch anti-detecção: oculta o Runtime.enable do CDP
# (o sensor.js do Akamai detecta esse domínio ligado). Usado só por
# scrapers/magalu.py no modo CDP — demais scrapers seguem no playwright stock.
rebrowser-playwright>=1.49.0
requests>=2.31.0
curl-cffi>=0.6.0
beautifulsoup4==4.12.3
pandas>=2.2.2
# Histórico frio em Parquet (utils/history) — Drive/disco
pyarrow>=15.0.0
# Google Drive como destino do histórico. Opcionais: sem eles o histórico
# cai no backend local (data/history/) sem quebrar a coleta.
google-api-python-client>=2.100.0
google-auth>=2.30.0
google-auth-oauthlib>=1.2.0
fake-useragent==1.5.1
python-dotenv==1.0.1
loguru==0.7.2
tenacity==8.3.0
supabase>=2.3.0
streamlit>=1.35.0
plotly>=5.20.0
anthropic>=0.40.0
# Screenshots (opcional — usado quando ENABLE_SCREENSHOTS=True)
Pillow>=10.0.0
filelock>=3.13.0
# Leitor de .xlsx (PriceTrack uploads via Streamlit Import History)
openpyxl>=3.1.0
