# Completions

Task completion documents. Each file records what was done, why, and what to watch for.

**Never auto-loaded** — only read when explicitly requested.

Use template: `.claude/templates/completion-template.md`
