# Claude AI related files
.claude/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
.venv-ci/
venv/

# Coletas e artefatos locais
output/
logs/
screenshots/
*.log

# Sessões de scraper (cookies Akamai validados — gerados em runtime)
data/magalu_session.json
data/magalu_chrome_profile/
data/chrome_profile/
utils/sessions/

# Histórico frio (Parquet) — vive no Google Drive, nunca no git.
# Aqui ficam só o backend local e o cache das partições baixadas.
data/history/

# Série de mais vendidos — acumulada em runtime; a fonte de verdade é a
# tabela `bestsellers` no Supabase.
data/bestsellers/

# Credenciais OAuth do Drive baixadas do Google Cloud Console
client_secret*.json
service_account*.json

# Ambiente
.env
